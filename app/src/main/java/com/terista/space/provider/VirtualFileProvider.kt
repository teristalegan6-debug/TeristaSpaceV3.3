package com.terista.space.provider

import androidx.core.content.FileProvider

/**
 * FileProvider for sharing files from virtual file system.
 */
class VirtualFileProvider : FileProvider() {
    // Uses default FileProvider implementation
}
